/** Page size for media library pickers (article cover/gallery, business wizard). */
export const MEDIA_PICKER_PAGE_SIZE = 24;

/** Safety cap on client-requested `limit` to prevent abuse via ?limit=999999. */
export const MEDIA_PICKER_MAX_LIMIT = 100;
