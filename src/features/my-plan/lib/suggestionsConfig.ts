/** Сколько раз можно нажать «Ещё варианты» за один заход (включая первую выдачу). */
export const MAX_SUGGESTION_BATCHES = 3;

/**
 * Сколько карточек в одной подборке — источник истины для Prisma `take` в
 * planSuggestions.service.ts (через /api/plan/suggestions), не для обрезки на рендере:
 * при квотировании по диапазонам возраста (M2.5) распределение должно считаться
 * по тому же набору, что покажется, а не по большему набору, обрезанному в UI.
 */
export const SUGGESTIONS_PER_BATCH = 3;
