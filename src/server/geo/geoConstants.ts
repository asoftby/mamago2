/** Default country for public city URLs and slug lookups (MVP: Belarus only). */
export const DEFAULT_COUNTRY_ISO = "BY";
export const DEFAULT_COUNTRY_SLUG = "belarus";

/** IANA timezone for calendar dates in public product flows (MVP: Belarus). */
export const DEFAULT_TZ = "Europe/Minsk";
