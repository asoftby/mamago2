# 3-Шаговый Онбординг при Регистрации из "Моего Плана"

## Как это работает

Когда пользователь не авторизован и кликает на "Мой план":

1. **Показывается превью** с описанием преимуществ
2. Пользователь кликает **"Начать планировать"**
3. Открывается модал регистрации с 3 шагами

## Шаг 1: Регистрация (Email + Пароль)

**Что видит пользователь:**
- Поле Email
- Поле Пароль (с кнопкой показать/скрыть)
- Поле Повторить пароль (с кнопкой показать/скрыть)
- Кнопки "Войти" / "Регистрация"
- Кнопка "Создать аккаунт"

**Валидация:**
- Email должен быть в формате `user@domain.com`
- Пароль минимум 8 символов
- Пароли должны совпадать
- Если email уже существует, показывается ошибка с ссылкой "Войти в существующий аккаунт"

**После успешной регистрации:**
- Автоматически переходит на Шаг 2

## Шаг 2: Добавить Ребёнка (Имя, Месяц, Год Рождения)

**Что видит пользователь:**
- Прогресс-бар: 2/3
- Поле "Имя ребёнка" (текстовое)
- Выпадающий список "Месяц рождения" (Январь-Декабрь)
- Выпадающий список "Год рождения" (последние 18 лет)
- Кнопка "Назад" (вернуться на Шаг 1)
- Кнопка "Продолжить"

**Валидация:**
- Все поля обязательны
- Если поле пусто, показывается ошибка "Заполните все поля"

**После заполнения:**
- Кликает "Продолжить"
- Переходит на Шаг 3

## Шаг 3: Выбрать Интересы → Вход

**Что видит пользователь:**
- Прогресс-бар: 3/3
- Заголовок "Что интересно ребёнку?"
- Сетка кнопок с интересами (2 колонки)
- Выбранные интересы подсвечены оранжевым
- Кнопка "Назад" (вернуться на Шаг 2)
- Кнопка "Создать план" (с загрузкой)
- Кнопка "Пропустить"

**Функциональность:**
- Клик на интерес = выбрать/отменить выбор
- Выбранные интересы показывают галочку
- Можно выбрать несколько интересов или пропустить

**После завершения:**
- Кликает "Создать план" или "Пропустить"
- Создаётся профиль ребёнка с выбранными интересами (или без них)
- Пользователь автоматически логинится
- Открывается "Мой план" с новым профилем ребёнка

## Технические детали

### Компоненты
- `DefaultAuthModal` - основной модал с 3 шагами
- `AuthStepContent` - Шаг 1 (регистрация)
- `ChildStepContent` - Шаг 2 (добавить ребёнка)
- `InterestsStepContent` - Шаг 3 (выбрать интересы)
- `OnboardingStepper` - визуальный прогресс-бар

### API Endpoints
- `POST /api/auth/complete-registration` - регистрация
- `POST /api/children` - создание профиля ребёнка
- `GET /api/interests` - загрузка интересов

### State Management
- `mode`: "login" | "register"
- `step`: "auth" | "child" | "interests"
- `withOnboarding`: true (включает 3-шаговый поток)

### Мобильный vs Десктоп
- **Мобильный**: Bottom Sheet (Sheet компонент)
- **Десктоп**: Dialog модал

## Поток данных

```
MyPlanWidget (не авторизован)
  ↓
MyPlanPreview (показать превью)
  ↓
"Начать планировать" клик
  ↓
DefaultAuthModal (withOnboarding=true)
  ↓
Шаг 1: Регистрация (email + пароль)
  ↓
Шаг 2: Добавить ребёнка (имя, месяц, год)
  ↓
Шаг 3: Выбрать интересы
  ↓
Создать профиль ребёнка
  ↓
Автоматический вход
  ↓
Открыть "Мой план"
```

## Обработка ошибок

**Шаг 1:**
- Некорректный email
- Пароль слишком короткий
- Пароли не совпадают
- Email уже существует (с ссылкой на вход)
- Ошибка сети

**Шаг 2:**
- Пустые поля
- Ошибка при создании профиля

**Шаг 3:**
- Ошибка при создании профиля ребёнка
- Ошибка при загрузке интересов

## Особенности

✅ Прогресс-бар показывает текущий шаг
✅ Кнопка "Назад" на каждом шаге (кроме первого)
✅ Кнопка закрытия (X) в углу
✅ Валидация на каждом шаге
✅ Понятные сообщения об ошибках
✅ Возможность пропустить интересы
✅ Автоматический вход после завершения
✅ Адаптивный дизайн (мобильный/десктоп)

## Статус

✅ **ГОТОВО** - 3-шаговый онбординг полностью реализован и работает
